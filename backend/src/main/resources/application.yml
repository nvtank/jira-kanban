server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/minijira
    username: minijira
    password: minijira123
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
services:
  db:
    image: postgres:16
    container_name: minijira_db
    environment:
      POSTGRES_DB: minijira
      POSTGRES_USER: minijira
      POSTGRES_PASSWORD: minijira123
    ports:
      - "5432:5432"
    volumes:
      - minijira_pg:/var/lib/postgresql/data

volumes:
  minijira_pg: